services:
  sentiment-analyzer:
    build: ./sentiment_analyzer
    ports:
      - "50051:50051"
    depends_on:
      - api-gateway
  api-gateway:
    build: ./api
    ports:
      - "8080:8080"
    depends_on:
      - sentiment-analyzer
  